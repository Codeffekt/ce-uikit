import { CeWidthDirective } from './width.directive';

describe('WidthDirective', () => {
  it('should create an instance', () => {
    const directive = new CeWidthDirective();
    expect(directive).toBeTruthy();
  });
});
