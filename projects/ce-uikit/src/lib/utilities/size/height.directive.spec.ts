import { CeHeightDirective } from './height.directive';

describe('HeightDirective', () => {
  it('should create an instance', () => {
    const directive = new CeHeightDirective();
    expect(directive).toBeTruthy();
  });
});
