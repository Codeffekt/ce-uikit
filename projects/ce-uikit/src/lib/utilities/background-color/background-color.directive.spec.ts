import { CeBackgroundColorDirective } from './background-color.directive';

describe('BackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CeBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
