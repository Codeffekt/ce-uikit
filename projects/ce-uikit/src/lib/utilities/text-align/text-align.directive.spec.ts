import { CeTextAlignDirective } from './text-align.directive';

describe('TextAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new CeTextAlignDirective();
    expect(directive).toBeTruthy();
  });
});
