import { CeDarkModeDirective } from './dark-mode.directive';

describe('DarkModeDirective', () => {
  it('should create an instance', () => {
    const directive = new CeDarkModeDirective();
    expect(directive).toBeTruthy();
  });
});
