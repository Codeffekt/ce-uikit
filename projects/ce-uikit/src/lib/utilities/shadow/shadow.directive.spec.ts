import { CeShadowDirective } from './shadow.directive';

describe('ShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new CeShadowDirective();
    expect(directive).toBeTruthy();
  });
});
