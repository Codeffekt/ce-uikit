import { CeCursorDirective } from './cursor.directive';

describe('CursorDirective', () => {
  it('should create an instance', () => {
    const directive = new CeCursorDirective();
    expect(directive).toBeTruthy();
  });
});
