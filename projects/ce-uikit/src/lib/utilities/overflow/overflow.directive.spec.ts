import { CeOverflowDirective } from './overflow.directive';

describe('OverflowDirective', () => {
  it('should create an instance', () => {
    const directive = new CeOverflowDirective();
    expect(directive).toBeTruthy();
  });
});
