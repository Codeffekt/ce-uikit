import { CeBorderRoundedDirective } from "./border-rounded.directive";

describe('BorderRoundedDirective', () => {
  it('should create an instance', () => {
    const directive = new CeBorderRoundedDirective();
    expect(directive).toBeTruthy();
  });
});
