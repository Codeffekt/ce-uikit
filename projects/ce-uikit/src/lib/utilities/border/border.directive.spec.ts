import { CeBorderDirective } from './border.directive';

describe('BorderWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new CeBorderDirective();
    expect(directive).toBeTruthy();
  });
});
