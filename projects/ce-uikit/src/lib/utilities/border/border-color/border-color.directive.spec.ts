import { CeBorderColorDirective } from './border-color.directive';

describe('BorderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CeBorderColorDirective();
    expect(directive).toBeTruthy();
  });
});
