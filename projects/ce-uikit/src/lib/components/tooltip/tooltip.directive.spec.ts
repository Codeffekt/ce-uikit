import { CeTooltipDirective } from './tooltip.directive';

describe('TooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CeTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
