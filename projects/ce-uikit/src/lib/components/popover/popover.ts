import { Injectable } from "@angular/core";
import { CeOverlay } from "../overlay";

@Injectable()
export class CePopover {

    constructor(private overlay: CeOverlay) {

    }

    // attach(component: ComponentType<T>) { }
}