import { CePositionDirective } from './position.directive';

describe('PositionDirective', () => {
  it('should create an instance', () => {
    const directive = new CePositionDirective();
    expect(directive).toBeTruthy();
  });
});
