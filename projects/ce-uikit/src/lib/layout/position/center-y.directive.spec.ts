import { CeCenterYDirective } from './center-y.directive';

describe('CenterYDirective', () => {
  it('should create an instance', () => {
    const directive = new CeCenterYDirective();
    expect(directive).toBeTruthy();
  });
});
