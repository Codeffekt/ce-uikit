import { CeCenterXDirective } from './center-x.directive';

describe('CenterXDirective', () => {
  it('should create an instance', () => {
    const directive = new CeCenterXDirective();
    expect(directive).toBeTruthy();
  });
});
