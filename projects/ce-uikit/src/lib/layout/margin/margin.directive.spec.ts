import { CeMarginDirective } from './margin.directive';

describe('MarginDirective', () => {
  it('should create an instance', () => {
    const directive = new CeMarginDirective();
    expect(directive).toBeTruthy();
  });
});
