import { CeJustifyContentDirective } from './justify-content.directive';

describe('JustifyContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CeJustifyContentDirective();
    expect(directive).toBeTruthy();
  });
});
