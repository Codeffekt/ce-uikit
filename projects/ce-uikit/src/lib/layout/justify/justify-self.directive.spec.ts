import { CeJustifySelfDirective } from './justify-self.directive';

describe('JustifySelfDirective', () => {
  it('should create an instance', () => {
    const directive = new CeJustifySelfDirective();
    expect(directive).toBeTruthy();
  });
});
