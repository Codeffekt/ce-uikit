import { JustifyItemsDirective } from './justify-items.directive';

describe('JustifyItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new JustifyItemsDirective();
    expect(directive).toBeTruthy();
  });
});
