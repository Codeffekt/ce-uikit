import { CePaddingDirective } from './padding.directive';

describe('PaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new CePaddingDirective();
    expect(directive).toBeTruthy();
  });
});
