import { CeGrowDirective } from './grow.directive';

describe('GrowDirective', () => {
  it('should create an instance', () => {
    const directive = new CeGrowDirective();
    expect(directive).toBeTruthy();
  });
});
