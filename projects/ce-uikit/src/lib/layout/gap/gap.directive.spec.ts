import { CeGapDirective } from './gap.directive';

describe('GapDirective', () => {
  it('should create an instance', () => {
    const directive = new CeGapDirective();
    expect(directive).toBeTruthy();
  });
});
