import { CeNoShrinkDirective } from './shrink.directive';

describe('ShrinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CeNoShrinkDirective();
    expect(directive).toBeTruthy();
  });
});
