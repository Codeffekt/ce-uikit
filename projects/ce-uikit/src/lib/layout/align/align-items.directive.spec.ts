import { CeAlignItemsDirective } from './align-items.directive';

describe('AlignItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new CeAlignItemsDirective();
    expect(directive).toBeTruthy();
  });
});
