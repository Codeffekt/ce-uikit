import { CeAlignSelfDirective } from './align-self.directive';

describe('AlignSelfDirective', () => {
  it('should create an instance', () => {
    const directive = new CeAlignSelfDirective();
    expect(directive).toBeTruthy();
  });
});
