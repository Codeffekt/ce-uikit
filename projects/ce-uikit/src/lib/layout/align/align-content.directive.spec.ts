import { CeAlignContentDirective } from './align-content.directive';

describe('AlignContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CeAlignContentDirective();
    expect(directive).toBeTruthy();
  });
});
